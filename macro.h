#ifndef MACRO_H
#define MACRO_H

// List of macros used in this project, sorted alphabetically

#define MAX(a,b) (((a) > (b)) ? (a) : (b))
#define EPOCH 10
#define BATCH 32
#define EARLY_EXIT_EPOCH 5

#endif
